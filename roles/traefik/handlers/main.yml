---
- name: Systemd daemon-reload
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart traefik
  ansible.builtin.systemd:
    name: traefik
    state: restarted
    daemon_reload: true

---
- name: Systemd daemon-reload
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart static_web
  ansible.builtin.systemd:
    name: static_web
    state: restarted
    daemon_reload: true
